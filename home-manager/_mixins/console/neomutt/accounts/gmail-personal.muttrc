# Gmail Personal
set imap_user = `cat $HOME/.config/sops-nix/secrets/email/google/personal/user`
set imap_pass = `cat $HOME/.config/sops-nix/secrets/email/google/personal/pass`
set folder = "imaps://imap.gmail.com:993"
set spoolfile = "+INBOX"
set record = "+[Gmail]/Sent Mail"
set postponed = "+[Gmail]/Drafts"
set trash = "+[Gmail]/Trash"

set smtp_url = "smtps://`cat $HOME/.config/sops-nix/secrets/email/google/personal/user`@smtp.gmail.com:465"
set smtp_pass = `cat $HOME/.config/sops-nix/secrets/email/google/personal/pass`

set from = `cat $HOME/.config/sops-nix/secrets/email/google/personal/user`
set realname = "Greg Burd"
set use_from = yes
set envelope_from = yes

set header_cache = "~/.cache/neomutt/gmail-personal/headers"
set message_cachedir = "~/.cache/neomutt/gmail-personal/bodies"

mailboxes +INBOX "+[Gmail]/Sent Mail" "+[Gmail]/Drafts" "+[Gmail]/Trash" "+[Gmail]/Spam"

set status_format = "[Gmail Personal] %f [Msgs:%?M?%M/?%m%?n? New:%n?]---(%s/%S)-%>-(%P)---"
color indicator black green
