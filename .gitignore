.direnv
result*
*.qcow2
.nixie
*~
.#*
\#*#

# Local config files
.bash_profile
.bashrc
.gitconfig
.gitmodules
.idea
.mcp.json
.profile
.ripgreprc
.zprofile
.zshrc
.claude/
.memelord/

# Amazon/internal overlay — MUST NOT be committed to this public repo.
# Amazon-internal steering, MCP endpoints, and Q config live outside the repo
# and are wired in via home.file mkOutOfStoreSymlink to local paths only.
*.local.nix
local/
**/amazon-builder-steering.md
.aws/amazonq/

# User-specific directories
arnold-config/
arnold-nix-setup
arnold-nvim

.memelord/

# Linux core dumps (kernel.core_pattern format)
core.[0-9]*
core-[0-9]*
