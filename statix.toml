# statix configuration
# Disable repeated_keys: false positive for NixOS module patterns
# where environment.* options are set across multiple logical blocks
disabled = ["repeated_keys"]
ignore = [".direnv", "examples"]
